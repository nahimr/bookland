<div class="card bg-dark" data-controller="auteur">
    <div class="card-body">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">{{ button_label|default('Ajouter un nouvel auteur') }}</button>
        {{ form_end(form) }}
    </div>
</div>